Ext.ns('App.Service');

Ext.Ajax.defaultHeaders = {Accept:'application/json'};

Ext.onReady(function() {
	
	var ServicePanel;
	var MainPanel;
	var InfoPanel;
	var ActionPanel;

    var viewport = new Ext.Viewport({
        layout: 'border',
        items: []
    });	
	
});